v0.55.0: tidy the HUD layout for the species panel

- moved the per-species counts into the top-right panel (under the population graph) and slid the narration panel down, so nothing overlaps
- all five species counts are now visible at a glance

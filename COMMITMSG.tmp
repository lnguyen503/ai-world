v0.54.0: cinematic camera finds the drama

- when you're not following anyone, the auto-orbit gently drifts its look-at toward a fresh kill for a few seconds, then eases back to centre
- subtle (slow lerp, partial offset) so it never snaps or hijacks the view; following a creature or stargazing takes over as before
- world now remembers the last kill location
